import { isEmpty } from 'lodash-es';
import { join } from 'path';

const a = {
	name: 'fnn',
	age: 18
};

const b = a?.age;

const name: any = ' wfld';

function test (param) {
	return param;
}


export { isEmpty };

const teste = isEmpty({});

console.log(join);
