{
	"dependencies": {
		"@vft/constants": "workspace:*",
		"@vue/tsconfig": "^0.1.3",
		"unbuild": "^1.1.2"
	},
	"scripts": {
		"stub": "pnpm run -r --parallel stub",
		"dev": "cd play && pnpm dev",
		"postinstall": "pnpm stub"
	}
}
