{
	"name": "demo-issue-template",
	"version": "1.0.0",
	"scripts": {
		"clean": "pnpm run -r clean && pnpm run clean:modules",
		"clean:modules": "rimraf node_modules"
	},
	"dependencies": {
		"rimraf": "^4.1.3"
	}
}
